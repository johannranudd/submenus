import styled from 'styled-components';

export const StyledDiv = styled.div`
  .hero {
    position: absolute;
    top: 0;
    z-index: -1;
    min-width: 100%;
    width: 180vh;
    overflow: hidden;
  }
  .homepage-container {
    height: 100vh;
    display: grid;
    place-content: center;
    width: 95%;
    margin: auto;
    .text-container {
    }
  }
`;
