import styled from 'styled-components';

export const StyledDiv = styled.div`
  .hero {
    position: absolute;
    top: 0;
    z-index: -1;
    min-width: 100vw;
    width: 150vh;
  }
  .homepage-container {
    width: 95%;
    margin: auto;
    .text-container {
    }
  }
`;
